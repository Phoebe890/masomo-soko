spring.application.name=eduhub-backend
server.port=${PORT:8080}

# --- DATABASE (Supabase) ---
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

# --- FILE UPLOADS ---
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# --- SESSION ---
server.servlet.session.cookie.name=JSESSIONID
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.same-site=none
server.servlet.session.timeout=30m
server.servlet.session.cookie.domain=masomosoko.co.ke
server.forward-headers-strategy=native

# --- GOOGLE AUTH ---
google.client.id=${GOOGLE_CLIENT_ID}

# --- ZOOM API ---
zoom.api.client-id=8YDG3EW2S0aVSE9PrveiNQ
zoom.api.client-secret=bsnMyzZtPK8F3L4FM77y48UHNhQ2AstS
zoom.redirect.uri=${BACKEND_URL}/api/auth/zoom/callback

# --- CLOUDINARY ---
cloudinary.cloud_name=dpwgloymm
cloudinary.api_key=517847241762175
cloudinary.api_secret=${CLOUDINARY_SECRET}

# --- M-PESA ---
mpesa.consumer.key=${MPESA_CONSUMER_KEY}
mpesa.consumer.secret=${MPESA_CONSUMER_SECRET}
mpesa.passkey=${MPESA_PASSKEY}
mpesa.stkpush.url=https://api.safaricom.co.ke/mpesa/stkpush/v1/processrequest
mpesa.auth.url=https://api.safaricom.co.ke/oauth/v1/generate?grant_type=client_credentials
mpesa.callback.url=${BACKEND_URL}/api/payment/callback

# Placeholders for Render Environment Variables
mpesa.shortcode=${MPESA_SHORTCODE}
mpesa.transaction.type=CustomerBuyGoodsOnline
mpesa.consumer.key=${MPESA_CONSUMER_KEY}
mpesa.consumer.secret=${MPESA_CONSUMER_SECRET}
mpesa.passkey=${MPESA_PASSKEY}
mpesa.callback.url=${BACKEND_URL}/api/payment/callback
# --- CORS ---
cors.allowed-origins=${FRONTEND_URL}
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true

# --- RABBITMQ ---
spring.rabbitmq.host=${RABBITMQ_HOST}
spring.rabbitmq.port=5671
spring.rabbitmq.username=${RABBITMQ_USERNAME}
spring.rabbitmq.password=${RABBITMQ_PASSWORD}
spring.rabbitmq.virtual-host=${RABBITMQ_VHOST}
spring.rabbitmq.ssl.enabled=true
spring.rabbitmq.ssl.algorithm=TLSv1.2

# --- JWT ---
application.security.jwt.secret-key=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
application.security.jwt.expiration=86400000
application.security.jwt.refresh-token.expiration=604800000